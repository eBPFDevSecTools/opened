<?xml version="1.0"?>
<doc>
<source file="/home/sayandes/opened_extraction/examples/bcc-libbpf-tools-main/compat.h" funcheader="static inline void *libbpf_reallocarray (void *ptr, size_t nmemb, size_t size)" startline="31" endline="44">
static inline void *libbpf_reallocarray (void *ptr, size_t nmemb, size_t size)
{
    size_t total;

#if __has_builtin(__builtin_mul_overflow)
    if (__builtin_mul_overflow (nmemb, size, &total))
        return NULL;

#else
    if (size == 0 || nmemb > ULONG_MAX / size)
        return NULL;
    total = nmemb * size;

#endif
    return realloc (ptr, total);
}
</source>
</doc>
